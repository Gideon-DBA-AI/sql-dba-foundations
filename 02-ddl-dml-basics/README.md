-- QUERY 1:CREATE A DATABASE CALLED 'SQL SCHOOL'

CREATE DATABASE SQLSCHOOL
--QUERY 2: CONNECT THE DATABASE

USE SQLSCHOOL
-- QUERY 3: CREATE A TABLE CALLED 'STUDENT-TABLE' WITH FEW COLMNS OF YOUR CHOICE

CREATE TABLE STUDENT_TABLE
(
STUDENT_TABLEREGNO INTEGER,  -- REGISTRATION NUMBERS OF THE STUDENTS
STUDENT_TABLENAMES CHARACTER(30),--NAMES OF STUDENTS
STUDENT_TABLEGRADE FLOAT    -- STUDENTS GRADE
)
--Create is a ddl statement used to define the database and table 
-- QUERY 4:INSERT DATA INTO ABOVE TABLE
-- Insert is a dml statement used to store data to the table.

INSERT STUDENT_TABLE VALUES (1, 'BOYLE',    3.2)
INSERT STUDENT_TABLE VALUES (2, 'MITCHELLE', 2.2)
INSERT STUDENT_TABLE VALUES (3, 'PALMER', 4.1)
--QUERY 5:REPORT DATA (SELECT DATA) FROM ABOVE TABLE

SELECT * FROM STUDENT_TABLE
-- QUERY 6: SAVE ABOVE SQL SCRIPT INTO A FILE.
--Select is a dql statement used to report 
